#!/usr/bin/env bash

# if [ "$HOSTNAME" = trsh ]; then
    # On laptop swap alt and win
    # setxkbmap -option altwin:swap_lalt_lwin
# fi

# Always bind Caps Lock to Escape
setxkbmap -option caps:escape
